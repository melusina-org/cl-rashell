name: 'Make Documentation'
on:
  - push
  - workflow_dispatch

jobs:
  make-documentation:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
    - uses: actions/checkout@v1
    - name: 'Setup Ubuntu'
      run: |
        sudo libexec/setup/ubuntu
    - name: 'Setup Quicklisp'
      run: |
        libexec/setup/quicklisp        
    - name: 'Run testsuite'
      run: |
        development/testsuite
    - name: 'Make documentation'
      run: |
        development/makedoc
    - uses: actions/upload-artifact@v3
      with:
        name: org.melusina.rashell.html
        path: obj/makedoc/org.melusina.rashell.html
    - uses: actions/upload-artifact@v3
      with:
        name: org.melusina.rashell.info
        path: obj/makedoc/org.melusina.rashell.info
    - uses: actions/upload-artifact@v3
      with:
        name: org.melusina.rashell.pdf
        path: obj/makedoc/org.melusina.rashell.pdf

